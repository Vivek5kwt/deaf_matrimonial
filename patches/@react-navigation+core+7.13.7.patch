diff --git a/node_modules/@react-navigation/core/lib/module/SceneView.js b/node_modules/@react-navigation/core/lib/module/SceneView.js
index 8e83282..450f12a 100644
--- a/node_modules/@react-navigation/core/lib/module/SceneView.js
+++ b/node_modules/@react-navigation/core/lib/module/SceneView.js
@@ -52,16 +52,23 @@ export function SceneView({
 
       // Before updating the state, cleanup any nested screen and state
       // This will avoid the navigator trying to handle them again
-      if (nextRoute.params && ('state' in nextRoute.params && typeof nextRoute.params.state === 'object' && nextRoute.params.state !== null || 'screen' in nextRoute.params && typeof nextRoute.params.screen === 'string')) {
+      const params = nextRoute.params;
+      const isValidParams = params && 
+          typeof params === 'object' && 
+          params !== null && 
+          !Array.isArray(params);
+      if (isValidParams && 
+          (Object.prototype.hasOwnProperty.call(params, 'state') && typeof params.state === 'object' && params.state !== null || 
+           Object.prototype.hasOwnProperty.call(params, 'screen') && typeof params.screen === 'string')) {
         // @ts-expect-error: we don't have correct type for params
         // eslint-disable-next-line @typescript-eslint/no-unused-vars
         const {
           state,
           screen,
-          params,
+          params: nestedParams,
           initial,
           ...rest
-        } = nextRoute.params;
+        } = params;
         if (Object.keys(rest).length) {
           return {
             ...nextRoute,
@@ -136,7 +143,100 @@ export function SceneView({
     getIsInitial,
     addOptionsGetter
   }), [routeState, getCurrentState, setCurrentState, getKey, setKey, getIsInitial, addOptionsGetter]);
-  const ScreenComponent = screen.getComponent ? screen.getComponent() : screen.component;
+  let ScreenComponent;
+  try {
+    const getComponentResult = screen.getComponent ? screen.getComponent() : screen.component;
+    
+    // Check if getComponent returned a promise (thenable)
+    if (getComponentResult && typeof getComponentResult.then === 'function') {
+      console.error('❌ Screen component is a promise (thenable) for screen:', screen.name, 'This is not supported. Component must be loaded synchronously.');
+      ScreenComponent = undefined;
+    } else {
+      ScreenComponent = getComponentResult;
+    }
+    
+    // Additional check: if ScreenComponent is still a promise after the check above
+    if (ScreenComponent && typeof ScreenComponent.then === 'function') {
+      console.error('❌ Screen component is still a promise after check for screen:', screen.name);
+      ScreenComponent = undefined;
+    }
+  } catch (error) {
+    console.error('❌ Error getting screen component for', screen.name, error);
+    ScreenComponent = undefined;
+  }
+  
+  // CRITICAL: Final validation - ensure ScreenComponent is NEVER undefined when used
+  // Safety check: ensure ScreenComponent is a valid React component
+  // Must be a function (component) or valid React element
+  // CRITICAL: Never allow undefined to be passed to React rendering
+  const isValidComponent = ScreenComponent !== undefined && 
+    ScreenComponent !== null &&
+    ScreenComponent !== false &&
+    typeof ScreenComponent !== 'undefined' &&
+    (typeof ScreenComponent === 'function' || 
+     (typeof ScreenComponent === 'object' && 
+      ScreenComponent !== null &&
+      ScreenComponent !== undefined &&
+      (ScreenComponent.$$typeof !== undefined || React.isValidElement(ScreenComponent))));
+  
+  // If component is invalid, log detailed error
+  if (!isValidComponent && !screen.children) {
+    console.error('❌❌❌ INVALID COMPONENT for screen:', screen.name);
+    console.error('   Component value:', ScreenComponent);
+    console.error('   Component type:', typeof ScreenComponent);
+    console.error('   Component === undefined:', ScreenComponent === undefined);
+    console.error('   Component === null:', ScreenComponent === null);
+    console.error('   Has getComponent:', !!screen.getComponent);
+    console.error('   Has component prop:', !!screen.component);
+    console.error('   Has children:', !!screen.children);
+    console.error('   screen.component value:', screen.component);
+    console.error('   screen.component type:', typeof screen.component);
+  }
+  
+  // NEVER render if component is invalid - return ErrorScreen instead of null
+  // Create a simple error component inline to avoid import issues
+  const ErrorFallback = React.createElement(React.Fragment, null,
+    React.createElement(require('react-native').View, {
+      style: { flex: 1, justifyContent: 'center', alignItems: 'center', padding: 20 }
+    },
+      React.createElement(require('react-native').Text, {
+        style: { fontSize: 18, fontWeight: 'bold', marginBottom: 10, color: '#000' }
+      }, 'Screen Error'),
+      React.createElement(require('react-native').Text, {
+        style: { fontSize: 14, color: '#666', textAlign: 'center' }
+      }, `Screen "${screen.name}" component is invalid. Check console for details.`)
+    )
+  );
+  
+  const renderContent = isValidComponent ? 
+    /*#__PURE__*/_jsx(ScreenComponent, {
+      navigation: navigation,
+      route: route
+    }) : 
+    (screen.children !== undefined ? screen.children({
+      navigation,
+      route
+    }) : ErrorFallback);
+  
+  // If we have nothing to render and no children, return ErrorFallback instead of null
+  if (!isValidComponent && !screen.children) {
+    return /*#__PURE__*/_jsx(NavigationStateContext.Provider, {
+      value: context,
+      children: /*#__PURE__*/_jsx(NavigationFocusedRouteStateContext.Provider, {
+        value: focusedRouteState,
+        children: /*#__PURE__*/_jsx(EnsureSingleNavigator, {
+          children: /*#__PURE__*/_jsx(StaticContainer, {
+            name: screen.name,
+            render: null,
+            navigation: navigation,
+            route: route,
+            children: ErrorFallback
+          })
+        })
+      })
+    });
+  }
+  
   return /*#__PURE__*/_jsx(NavigationStateContext.Provider, {
     value: context,
     children: /*#__PURE__*/_jsx(NavigationFocusedRouteStateContext.Provider, {
@@ -144,16 +244,10 @@ export function SceneView({
       children: /*#__PURE__*/_jsx(EnsureSingleNavigator, {
         children: /*#__PURE__*/_jsx(StaticContainer, {
           name: screen.name,
-          render: ScreenComponent || screen.children,
+          render: isValidComponent ? ScreenComponent : (screen.children || null),
           navigation: navigation,
           route: route,
-          children: ScreenComponent !== undefined ? /*#__PURE__*/_jsx(ScreenComponent, {
-            navigation: navigation,
-            route: route
-          }) : screen.children !== undefined ? screen.children({
-            navigation,
-            route
-          }) : null
+          children: renderContent
         })
       })
     })
diff --git a/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js b/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js
index 1c8d466..1618110 100644
--- a/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js
+++ b/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js
@@ -487,12 +487,12 @@ export function useNavigationBuilder(createRouter, options) {
   });
   const onUnhandledActionParent = React.useContext(UnhandledActionContext);
   const onUnhandledAction = useLatestCallback(action => {
-    if (options.UNSTABLE_routeNamesChangeBehavior === 'lastUnhandled' && action.type === 'NAVIGATE' && action.payload != null && 'name' in action.payload && typeof action.payload.name === 'string' && !routeNames.includes(action.payload.name)) {
+    if (options.UNSTABLE_routeNamesChangeBehavior === 'lastUnhandled' && action.type === 'NAVIGATE' && action.payload != null && typeof action.payload === 'object' && action.payload !== null && Object.prototype.hasOwnProperty.call(action.payload, 'name') && typeof action.payload.name === 'string' && !routeNames.includes(action.payload.name)) {
       const state = {
         routes: [{
           name: action.payload.name,
-          params: 'params' in action.payload && typeof action.payload.params === 'object' && action.payload.params !== null ? action.payload.params : undefined,
-          path: 'path' in action.payload && typeof action.payload.path === 'string' ? action.payload.path : undefined
+          params: Object.prototype.hasOwnProperty.call(action.payload, 'params') && typeof action.payload.params === 'object' && action.payload.params !== null ? action.payload.params : undefined,
+          path: Object.prototype.hasOwnProperty.call(action.payload, 'path') && typeof action.payload.path === 'string' ? action.payload.path : undefined
         }]
       };
       setUnhandledState(state);
diff --git a/node_modules/@react-navigation/core/src/SceneView.tsx b/node_modules/@react-navigation/core/src/SceneView.tsx
index ca608bd..64c4f29 100644
--- a/node_modules/@react-navigation/core/src/SceneView.tsx
+++ b/node_modules/@react-navigation/core/src/SceneView.tsx
@@ -84,17 +84,23 @@ export function SceneView<
 
         // Before updating the state, cleanup any nested screen and state
         // This will avoid the navigator trying to handle them again
+        const params = nextRoute.params;
+        const isValidParams =
+          params &&
+          typeof params === 'object' &&
+          params !== null &&
+          !Array.isArray(params);
         if (
-          nextRoute.params &&
-          (('state' in nextRoute.params &&
-            typeof nextRoute.params.state === 'object' &&
-            nextRoute.params.state !== null) ||
-            ('screen' in nextRoute.params &&
-              typeof nextRoute.params.screen === 'string'))
+          isValidParams &&
+          ((Object.prototype.hasOwnProperty.call(params, 'state') &&
+            typeof params.state === 'object' &&
+            params.state !== null) ||
+            (Object.prototype.hasOwnProperty.call(params, 'screen') &&
+              typeof params.screen === 'string'))
         ) {
           // @ts-expect-error: we don't have correct type for params
           // eslint-disable-next-line @typescript-eslint/no-unused-vars
-          const { state, screen, params, initial, ...rest } = nextRoute.params;
+          const { state, screen, params: nestedParams, initial, ...rest } = params;
 
           if (Object.keys(rest).length) {
             return { ...nextRoute, params: rest };
@@ -206,9 +212,60 @@ export function SceneView<
     ]
   );
 
-  const ScreenComponent = screen.getComponent
-    ? screen.getComponent()
-    : screen.component;
+  let ScreenComponent: React.ComponentType<any> | undefined;
+  try {
+    ScreenComponent = screen.getComponent
+      ? screen.getComponent()
+      : screen.component;
+
+    // Check if getComponent returned a promise (thenable)
+    if (ScreenComponent && typeof (ScreenComponent as any).then === 'function') {
+      console.error(
+        'Screen component is a promise (thenable) for screen:',
+        screen.name,
+        'This is not supported. Component must be loaded synchronously.'
+      );
+      ScreenComponent = undefined;
+    }
+  } catch (error) {
+    console.error('Error getting screen component for', screen.name, error);
+    ScreenComponent = undefined;
+  }
+
+  // Safety check: ensure ScreenComponent is a valid React component
+  // Must be a function (component) or valid React element
+  // CRITICAL: Never allow undefined to be passed to React rendering
+  const isValidComponent =
+    ScreenComponent !== undefined &&
+    ScreenComponent !== null &&
+    ScreenComponent !== false &&
+    (typeof ScreenComponent === 'function' ||
+      (typeof ScreenComponent === 'object' &&
+        ScreenComponent !== null &&
+        ((ScreenComponent as any).$$typeof !== undefined ||
+          React.isValidElement(ScreenComponent))));
+
+  // If component is invalid, log detailed error
+  if (!isValidComponent && !screen.children) {
+    console.error('❌ INVALID COMPONENT for screen:', screen.name);
+    console.error('   Component value:', ScreenComponent);
+    console.error('   Component type:', typeof ScreenComponent);
+    console.error('   Has getComponent:', !!screen.getComponent);
+    console.error('   Has component prop:', !!screen.component);
+    console.error('   Has children:', !!screen.children);
+  }
+
+  // NEVER render if component is invalid - return null instead
+  const renderContent = isValidComponent ? (
+    <ScreenComponent navigation={navigation} route={route} />
+  ) : screen.children !== undefined ? (
+    screen.children({ navigation, route })
+  ) : null;
+
+  // If we have nothing to render and no children, return null to prevent React error
+  if (!isValidComponent && !screen.children) {
+    return null;
+  }
 
   return (
     <NavigationStateContext.Provider value={context}>
@@ -216,15 +273,11 @@ export function SceneView<
         <EnsureSingleNavigator>
           <StaticContainer
             name={screen.name}
-            render={ScreenComponent || screen.children}
+            render={isValidComponent ? ScreenComponent : screen.children || null}
             navigation={navigation}
             route={route}
           >
-            {ScreenComponent !== undefined ? (
-              <ScreenComponent navigation={navigation} route={route} />
-            ) : screen.children !== undefined ? (
-              screen.children({ navigation, route })
-            ) : null}
+            {renderContent}
           </StaticContainer>
         </EnsureSingleNavigator>
       </NavigationFocusedRouteStateContext.Provider>
diff --git a/node_modules/@react-navigation/core/src/useNavigationBuilder.tsx b/node_modules/@react-navigation/core/src/useNavigationBuilder.tsx
index 7ef1ae6..700f0f3 100644
--- a/node_modules/@react-navigation/core/src/useNavigationBuilder.tsx
+++ b/node_modules/@react-navigation/core/src/useNavigationBuilder.tsx
@@ -835,7 +835,9 @@ export function useNavigationBuilder<
       options.UNSTABLE_routeNamesChangeBehavior === 'lastUnhandled' &&
       action.type === 'NAVIGATE' &&
       action.payload != null &&
-      'name' in action.payload &&
+      typeof action.payload === 'object' &&
+      action.payload !== null &&
+      Object.prototype.hasOwnProperty.call(action.payload, 'name') &&
       typeof action.payload.name === 'string' &&
       !routeNames.includes(action.payload.name)
     ) {
@@ -844,13 +846,13 @@ export function useNavigationBuilder<
           {
             name: action.payload.name,
             params:
-              'params' in action.payload &&
+              Object.prototype.hasOwnProperty.call(action.payload, 'params') &&
               typeof action.payload.params === 'object' &&
               action.payload.params !== null
                 ? action.payload.params
                 : undefined,
             path:
-              'path' in action.payload &&
+              Object.prototype.hasOwnProperty.call(action.payload, 'path') &&
               typeof action.payload.path === 'string'
                 ? action.payload.path
                 : undefined,
